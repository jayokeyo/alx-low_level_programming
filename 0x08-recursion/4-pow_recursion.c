#include "main.h"
/**
 * _pow_recursion - evaluates pow(x, y)
 * @x: base integer
 * @y: power index
 * Return: x raised to y
 */
int _pow_recursion(int x, int y)
{
	int ans;

	ans = x ** y;
	return (ans);
}
