#include "main.h"
/**
 * mul - multiplies two numbers
 * @num: array of two integers
 * Return: num[0] * num[1]
 */
int mul(int num[2])
{
	if (num[0] == NULL || num[1] == NULL)
	{
		print("Error\n");
		exit(98);
	}
}
