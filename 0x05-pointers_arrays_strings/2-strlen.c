#include <string.h>
#include "main.h"
/**
 * _strlen - returns the length of a string
 * @s: string input to function
 * Return: length of the string
 */
int _strlen(char *s)
{
	int a;
	a = *s;

	return (strlen(a));
}
